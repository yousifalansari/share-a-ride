<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Rides</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>All Available Rides</h1>
    <a href="/rides/new">Create New Ride</a>

    <ul>
      <% rides.forEach((ride) => { %>
      <li>
        Trip from <%= ride.origin %> to <%= ride.destination %> on <%= new Date(ride.departureDateTime).toLocaleString() %>
        driven by <%= ride.driverId.username %> |
        <a href="/rides/<%= ride._id %>">View Details</a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Rides</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>All Available Rides</h1>
    <a href="/rides/new">Create New Ride</a>

    <ul>
      <% rides.forEach((ride) => { %>
      <li>
        Trip from <%= ride.origin %> to <%= ride.destination %> on <%= new Date(ride.departureDateTime).toLocaleString() %>
        driven by <%= ride.driverId.username %> |
        <a href="/rides/<%= ride._id %>">View Details</a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
